# GovPlot Tracker — Python dependencies v3.3

# ── Scraper ─────────────────────────────────────────────────────────────────
requests==2.32.3
beautifulsoup4==4.12.3
lxml==5.2.1

# httpx: alternative HTTP client — different TLS fingerprint than requests
# bypasses some WAFs and bot-detection on government portals
httpx==0.27.0
httpx>=0.27.0

# Playwright: PRIMARY JS scraper — downloads its own bundled Chromium
# Run: python -m playwright install chromium  (done in workflow step 6)
# Eliminates "no chrome binary" error — Playwright never uses system Chrome
playwright==1.44.0

# Selenium: SECONDARY JS scraper (legacy fallback)
# Requires google-chrome-stable installed (workflow step 3)
selenium==4.21.0

# ── Backend ──────────────────────────────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.30.0
sqlalchemy==2.0.30
pydantic[email]==2.7.1
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.9
python-dotenv==1.0.1

# ── Notifications ────────────────────────────────────────────────────────────
sendgrid==6.11.0
twilio==9.1.0
firebase-admin==6.5.0
python-telegram-bot==21.3

# ── Database ─────────────────────────────────────────────────────────────────
psycopg2-binary==2.9.9
supabase==2.4.6

#------LoginPasswordencryption-------------
bcrypt
python-jose[cryptography]

